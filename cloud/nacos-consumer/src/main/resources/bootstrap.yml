server:
  port: 8080
spring:
  application:
    name: nacos-config
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yml
        # 对应nacos中的namespace属性
        # namespace: public(或者是uuid)
        # 对应nacos中的group属性
        # group: dev_group